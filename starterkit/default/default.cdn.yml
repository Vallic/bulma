# Name of the CDN provider.
name: jsdelivr
# Home page URL of the CDN provider.
home: https://www.jsdelivr.com
api:
  bulma:
    # Root URL of files, including [version] placeholder for version string.
    file_root: https://cdn.jsdelivr.net/npm/bulma@[version]
    # URL of version data request.
    version: https://data.jsdelivr.com/v1/package/npm/bulma
  bulmaswatch:
    # Root URL of files, including [version] placeholder for version string.
    file_root: https://cdn.jsdelivr.net/npm/bulmaswatch@[version]
    # URL of version data request.
    version: https://data.jsdelivr.com/v1/package/npm/bulmaswatch
